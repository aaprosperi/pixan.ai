# Force npm to install only compatible platform-specific packages
optional=true
platform=linux
arch=x64

# Use legacy peer deps to avoid conflicts
legacy-peer-deps=true

# Increase timeout for slow connections
fetch-timeout=600000
